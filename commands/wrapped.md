---
description: Show your Weekly Wrapped — battle stats for the last 7 days
allowed-tools: Bash
---

Run this command and show its full output to the user verbatim, nothing else:

```
node "${CLAUDE_PLUGIN_ROOT}/scripts/wrapped.js"
```
