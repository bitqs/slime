{
  "name": "slime",
  "version": "0.1.0",
  "private": true,
  "description": "Your work goals are the bosses. A zero-impact RPG layer over Claude Code.",
  "license": "MIT",
  "dependencies": {},
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.6.0"
  },
  "scripts": {
    "test": "node --test test/",
    "typecheck": "tsc -p ."
  }
}
